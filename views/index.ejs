<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Nail Art Gallery</title>
</head>
<body>
<%- include('partials/header') %>

<div class="container">
  <h2>Filter by Category</h2>
  <ul>
    <li><a href="/designs/category/gel">Gel</a></li>
    <li><a href="/designs/category/acrylic">Acrylic</a></li>
    <li><a href="/designs/category/dipping-powder">Dipping-Powder</a></li>
  </ul>

  <h2>Designs</h2>
  <% if (designs.length > 0) { %>
    <ul>
      <% designs.forEach(design => { %>
        <li>
          <a href="/designs/<%= design._id %>"><%= design.title %></a>
          by <a href="/users/<%= design.author._id %>"><%= design.author.username %></a>
        </li>
      <% }) %>
    </ul>
  <% } else { %>
    <p>No designs found in this category.</p>
  <% } %>
</div>

<%- include('partials/footer') %>
</body>
</html>
